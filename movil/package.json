{
  "name": "@comeia/movil",
  "version": "0.1.0",
  "private": true,
  "description": "Envuelve el panel de comeIA en una app de Android.",
  "scripts": {
    "preparar": "node preparar.mjs",
    "apk": "node preparar.mjs && cd android && ./gradlew assembleDebug"
  },
  "devDependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  }
}
